import random.Scanner;

public class RandomSubstraction{
public static void main(String[] args){

Random random = new Random(System.in);
System.out.println("Enter questions");
System.out.println("Enter answer");

int number = input.nextInt;

String displays = Calculate(numbers);

 }
System.out.println(answer);
}
public class static Calculate(int number){

int displayQuestions += (1, 100);
int displayAnswers = (1, 10) / 1000;

for(count = 0; count = random.randint; count--);

while
    (displayQuestions >= 100){
    displayAnswers -= 10;

int displayQusetions = displayAnswer -< 100;
    if("Enter questions") return (random questions);
    if(2 attempts) return ("Final attempt!")
}

